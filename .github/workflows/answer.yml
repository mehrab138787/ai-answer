name: Get AI Answer

on:
  workflow_dispatch:
    inputs:
      prompt:
        description: 'سوال خود را بنویسید'
        required: true
        type: string

jobs:
  get_answer:
    runs-on: ubuntu-latest
    steps:
      - name: Call OpenRouter
        run: |
          curl -s -X POST https://openrouter.ai/api/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.OPENROUTER_API_KEY }}" \
            -d '{
              "model": "openrouter/free",
              "messages": [{"role": "user", "content": "'"${{ github.event.inputs.prompt }}"'"}]
            }' > full.json
          
          # استخراج پاسخ با jq و ذخیره در فایل
          jq -r '.choices[0].message.content // "خطا: پاسخی دریافت نشد"' full.json > answer.txt
          
      - name: Upload answer as artifact
        uses: actions/upload-artifact@v4
        with:
          name: ai-answer
          path: answer.txt
